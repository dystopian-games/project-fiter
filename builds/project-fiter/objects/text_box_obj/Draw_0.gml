/// @description Draws itself with the text
/* the text is held in a variable, text_box_text
 * remember to assign this variable to the text
*/
// draw itself so it can add text
//draw_self();

// assign location and font
//draw_set_valign(fa_bottom);
//draw_set_halign(fa_left);
//draw_set_font(Slimytext);
//show_message(text_box_text);
// set the readable text
//draw_text_ext(20, 800, text_box_text, 2, 1600);
//show_message("After")
/*while (instance_exists(text_box_obj)) {
	if keyboard_check_pressed(vk_space) {
		if (finished) {
			// if it has been finished
			instance_destroy();
		} else {
			finish = true;
		}
	}
}
*/